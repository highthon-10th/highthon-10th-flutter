import 'package:flutter/material.dart';

class EventTags extends StatelessWidget {
  const EventTags({super.key});

  @override
  Widget build(BuildContext context) {
    return Row(
      children: [
        
      ],
    );
  }
}
